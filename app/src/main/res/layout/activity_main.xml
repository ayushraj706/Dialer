<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_coordinator"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 1. MAIN CONTENT AREA (List & Tabs) -->
    <!-- HATA DIYA: app:layout_behavior. Ab ye screen ke top se bottom tak fail jayega -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/mainHolder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:scrollbars="none">

        <!-- BLUR TARGET (Jiske andar ViewPager list hai) -->
        <eightbitlab.com.blurview.BlurTarget
            android:id="@+id/blurTarget"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- MAGIC TRICK: clipToPadding & paddings added here! -->
            <!-- Isse koi contact top/bottom bar ke peeche hamesha ke liye nahi chhupega -->
            <com.goodwy.commons.views.MyViewPager
                android:id="@+id/viewPager"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingTop="85dp"
                android:paddingBottom="100dp" />

        </eightbitlab.com.blurview.BlurTarget>

        <!-- Top segmented pill (used when the "top tabs" layout mode is active) -->
        <RelativeLayout
            android:id="@+id/mainTopTabsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <eightbitlab.com.blurview.BlurView
                android:id="@+id/mainTopTabsBackground"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:layout_alignTop="@+id/mainTopTabsHolder"
                android:layout_alignBottom="@+id/mainTopTabsHolder"
                android:layout_alignStart="@+id/mainTopTabsHolder"
                android:layout_alignEnd="@+id/mainTopTabsHolder"
                android:layout_marginTop="-3dp"
                android:layout_marginBottom="-3dp"
                android:layout_marginStart="-3dp"
                android:layout_marginEnd="-3dp"
                android:background="@drawable/glass_clip_shape"
                android:foreground="@drawable/glass_border_stroke"
                android:elevation="12dp"
                tools:ignore="Orientation"/>

            <com.google.android.material.tabs.TabLayout
                android:id="@+id/mainTopTabsHolder"
                android:layout_width="wrap_content"
                android:layout_height="26dp"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="6dp"
                android:layout_marginBottom="12dp"
                android:background="@null"
                android:elevation="16dp"
                app:tabIndicator="@drawable/tab_indicator"
                app:tabIndicatorAnimationMode="elastic"
                app:tabIndicatorColor="@android:color/white"
                app:tabIndicatorFullWidth="true"
                app:tabIndicatorGravity="stretch"
                app:tabIndicatorHeight="48dp"
                app:tabMaxWidth="@dimen/tab_layout_width"
                app:tabMinWidth="@dimen/tab_layout_width"
                app:tabMode="scrollable"
                app:tabRippleColor="@null"
                app:tabSelectedTextColor="@android:color/white"/>
        </RelativeLayout>

        <!-- Floating Action Buttons -->
        <com.goodwy.commons.views.MyFloatingActionButton
            android:id="@+id/main_dialpad_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="@dimen/bigger_margin"
            android:layout_marginBottom="@dimen/activity_margin"
            android:contentDescription="@string/dialpad"
            android:elevation="@dimen/medium_margin"
            android:padding="@dimen/activity_margin"
            android:src="@drawable/ic_dialpad_vector"
            app:backgroundTint="@color/color_primary"
            app:layout_constraintBottom_toTopOf="@+id/mainTabsGlassWrapper"
            app:layout_constraintEnd_toEndOf="parent"
            app:rippleColor="@color/pressed_item_foreground"/>

        <com.goodwy.commons.views.MyFloatingActionButton
            android:id="@+id/mainCallButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="@dimen/bigger_margin"
            android:layout_marginBottom="@dimen/activity_margin"
            android:contentDescription="@string/dialpad"
            android:elevation="@dimen/medium_margin"
            android:padding="@dimen/activity_margin"
            android:scaleX="0.6"
            android:scaleY="0.6"
            android:alpha="0"
            android:src="@drawable/ic_phone_vector"
            app:backgroundTint="@color/color_primary"
            app:layout_constraintBottom_toTopOf="@+id/main_dialpad_button"
            app:layout_constraintEnd_toEndOf="parent"
            app:rippleColor="@color/pressed_item_foreground"
            tools:alpha="1"/>

        <!-- BOTTOM NAV GLASS -->
        <FrameLayout
            android:id="@+id/mainTabsGlassWrapper"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <eightbitlab.com.blurview.BlurView
                android:id="@+id/mainTabsBlurView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/glass_clip_shape"
                android:foreground="@drawable/glass_border_stroke"
                android:elevation="4dp"/>

            <com.google.android.material.tabs.TabLayout
                android:id="@+id/mainTabsHolder"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@android:color/transparent"
                android:elevation="8dp"
                app:tabIndicator="@null"
                app:tabMinWidth="150dp"
                app:tabRippleColor="@null"/>
        </FrameLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- 2. TOP BAR (TELEGRAM STYLE GLASS) -->
    <!-- Ye code ab mainHolder ke baad hai, isliye ye screen ke upar overlap/float karega -->
    <eightbitlab.com.blurview.BlurView
        android:id="@+id/topMenuBlurView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top">

        <com.goodwy.commons.views.MySearchMenu
            android:id="@+id/mainMenu"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:elevation="0dp" />
            
    </eightbitlab.com.blurview.BlurView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
